create table utilization(
	svc text,
	svvd text,
	region text,
	local_teu smallint,
	trans_teu smallint,
	modified timestamp without time zone,
	reporter text
)；

create table allocation(
    svvd text,
    region text,
    local_teu smallint,
    trans_teu smallint,
	modified timestamp without time zone,
	reporter text);

